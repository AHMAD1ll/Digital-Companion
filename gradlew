#!/usr/bin/env sh\n\nDEFAULT_JVM_OPTS="\"-Xmx64m\" \"-Xms64m\""\n\nAPP_HOME=$(dirname "$(realpath "$0")")\n\nif [ -z "$JAVA_HOME" ]; then\n  if [ -x "$APP_HOME/jre/bin/java" ]; then\n    JAVA_HOME="$APP_HOME/jre"\n  elif [ -x "/usr/bin/java" ]; then\n    JAVA_HOME="/usr"\n  fi\nfi\n\nif [ -n "$JAVA_HOME" ]; then\n  JAVA_CMD="$JAVA_HOME/bin/java"\nelse\n  JAVA_CMD="java"\nfi\n\nexec "$JAVA_CMD" $DEFAULT_JVM_OPTS -Dorg.gradle.appname="gradlew" -classpath "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" org.gradle.wrapper.GradleWrapperMain "$@"
